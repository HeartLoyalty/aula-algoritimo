import input from 'readline-sync'

let numero = input.questionInt('Digite qualquer numero:');
while (numero != 0) {
    
    if (numero != 0) {
        let position = 1;
        console.log(numero + 'posição' + position);
        position++;
        
        
        
    }
}